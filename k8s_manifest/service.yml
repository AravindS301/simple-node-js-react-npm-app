apiVersion: v1
kind: Service
metadata:
  name: react-service
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: react-app
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 3000
